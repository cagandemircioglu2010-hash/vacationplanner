# Server configuration
PORT=3000

# SMTP configuration for password reset emails
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
# Set to true only if your SMTP server requires plain connections without STARTTLS.
# Leaving this unset keeps connections encrypted.
# SMTP_ALLOW_INSECURE=false
# Set to false only if your SMTP server uses a self-signed certificate.
# SMTP_TLS_REJECT_UNAUTHORIZED=true
SMTP_USER=apikey
SMTP_PASS=secret
EMAIL_FROM=no-reply@example.com
EMAIL_FROM_NAME=Vacation Planner Support
